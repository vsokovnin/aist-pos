# Хэндофф: аудит модели AIST POS

**Дата:** 2026-08-14 · **Состояние:** аудит пройден целиком, ждёт приёмки Виктора
**Спек:** `specs/2026-08-13-model-audit-design.md` · **План:** `specs/2026-08-13-model-audit-plan.md`
**Реестр исполнения (git-ignored):** `.superpowers/sdd/2026-08-13-model-audit-plan/progress.md`

Документ написан для сессии, которая продолжит работу после сжатия контекста. Всё, что нужно
для продолжения, — здесь; читать переписку заново не требуется.

---

## 1. Где мы

Модель зрелости AIST POS (13 рабочих задач × 18 способностей × 5 уровней) прошла полный аудит:
деривация состава из задач, судейство, состязательный пас, сертификация, документы защиты.
Ветка `main`, дерево чистое, 26 коммитов от `9773d4d` до `d6c9e99`.

**Все гейты зелёные:**
```bash
./scripts/pack.sh                          # 8 гейтов сборки + пакет
python3 scripts/audit/certify.py 2026-08-14   # 8 тестов устойчивости
python3 scripts/audit/validate-traces.py      # трассы ≡ матрица
```

**Текущее состояние матрицы:** 51 ячейка порога входа, 63 яруса надёжности, 3 гигиены
(`git`, `naming`, `source_map`). Версия навыка пока **2.14.6** — не поднята намеренно.

---

## 2. Что изменено в продукте, а что нет

**Изменено (едет в пакет пользователю):**

| Файл | Что |
|---|---|
| `rubric/job-sets.json` | матрица переписана: `needs` = порог входа; новые `hardens` (ярус надёжности) и top-level `hygiene` |
| `rubric/aist-pos-rubric.yaml` | `connectors` → направление `capture`; из шапки удалены списки `caps[]` (двойная запись) |
| `rubric/playbook.json` | карта задачи «День начинается с одной сводки» синхронизирована (5 способностей) |
| `examples/profile.example.json` | `dir` у connectors выровнен |
| `scripts/pack.sh` | +3 гейта: согласованность направлений, трассировка, (эхо — пока НЕ включён) |
| `docs/methodology.md` | НОВЫЙ, публичный, черновик |
| `docs/level-grammar.md` | НОВЫЙ, публичный, черновик |

**НЕ изменено:**

- **18 вопросов и 90 формулировок уровней** — целы. Правки лежат предложениями в
  `derivation/rubric-edits-proposed.md` (21 штука), ждут решения Виктора.
- **Поведение навыка вокруг ярусов.** Проверено grep'ом: `hardens` и `hygiene` **не читает ни один
  сценарий** (`workflows/*.md`, `scripts/make-page.py`, `scripts/make-demo.py`). Пользователь уже
  почувствует снизившийся порог входа (задачи ближе), но блока «основа системы» и двухосевой
  готовности («заработает / перестанет подводить») НЕ увидит. Это отдельная работа на ~полдня.

---

## 3. Пять открытых решений Виктора

### 3.1 Предмет способности «Вводные под задачу» (`context_seed`) — РЕШЕНИЕ, не одобрение

Вопрос и уровни меряют **материалы под конкретную задачу**, а осмотр (`inspect`) и рецепт
(`to_L3`) — **постоянные вводные о человеке** («кто я, в какой роли, чего не делать никогда»).
Это разные предметы. Следствия, подтверждённые аудитом: способность 4 раза проиграла
территориальные споры судьям (задачи `draft` и `challenge`, в каждой по две устоявшие атаки),
а в слепой деривации `draft` её вывел 1 агент из 3.

Два варианта с последствиями — раздел D в `derivation/rubric-edits-proposed.md`.
**От этого решения зависят 2 правки из пачки** (раздел B, уровни 3 и 5 этой способности).

### 3.2 Пачка из 21 правки формулировок

`derivation/rubric-edits-proposed.md`: раздел A — 13 снятий эха (правился вариант ответа, не вопрос),
раздел B — 6 правок грамматики ступени, раздел C — 2 правки формы вопроса. Каждая в формате
«сейчас (файл:строка) → проблема → предлагаю → что меняется». Все новые формулировки прогнаны
тем же биграммным алгоритмом, что и гейт.

Одобрить пачкой или пройти поштучно.

### 3.3 Две отложенные задачи

`assistant` («Ассистент работает в вашей системе») и `quarter` («Квартал закрыт») выведены из
применения паса Б решением Виктора: их порог падал до двух способностей. **9 отложенных атак**
помечены `decision: defer` в `derivation/attacks.json`.

### 3.4 Пять рецептов перехода на четвёртый уровень

Способности `capture`, `connectors`, `doc_source`, `graph`, `handover` требуются на уровне 4 через
ярус надёжности, но рецепта `to_L4` нет ни у одной (плюс `research` L5 — рецептов L5 нет вообще).
Дыры **приняты явно** в `derivation/accepted-gaps.json` (⚠ в сертификации, не роняют прогон).
Решение: писать рецепты (пользовательский контент + пошаговые инструкции в `guides.json`)
или оставить честной дырой в методологии.

### 3.5 Мелочи

- **Округление стадии на границе.** Правила для ровно 2.5 / 3.5 нет нигде; кода скоринга в репо
  тоже нет — считает LLM по тексту инструкции. Названо слабым местом в `derivation/scoring-defense.md`.
- **Глоссарий не знает трёх ярусов.** `docs/glossary.md` не содержит терминов «порог входа»,
  «ярус надёжности», «гигиена системы» — вписать при следующей правке.
- **`promises` / `output_form`.** Принцип «форма результата на входе только для артефактов наружу»
  предполагает вынос, но контрпримера никто не строил — проверить отдельно.

---

## 4. Процедура выката (после решений)

**Шаг 1 (Виктор, ~30 мин):** прогнать `pack.sh` + `certify.py`; прочитать `docs/methodology.md`
(разделы 1, 2, 8 — там его голос и его решения); ответить на пять пунктов раздела 3.

**Шаг 2 (сессия, ~1 час):**
1. применить решение по `context_seed` (если «свести к предмету рецепта» — переписать 2 правки раздела B);
2. применить 21 правку в `rubric/aist-pos-rubric.yaml`;
3. `python3 scripts/audit/echo-check.py` → должен стать зелёным;
4. включить гейт эха в `scripts/pack.sh` (строка-памятка в конце `echo-check.py`);
5. `./scripts/pack.sh` + `certify.py` + `validate-traces.py` → всё зелёное;
6. поднять версию в `SKILL.md` до **2.15.0**, строка в `CHANGELOG.md`;
7. пересобрать демо и плейбук: `python3 scripts/make-demo.py`, `python3 scripts/make-playbook.py playbook.html`;
8. коммит; пуш и релиз — только по слову Виктора.

**Шаг 3 (Виктор, 5 мин):** удалить старые копии навыка из Claude Cowork, поставить новый архив
`dist/aist-pos.skill`, в новом разговоре спросить версию — должна назваться 2.15.0.

---

## 5. Инструментарий (всё в `scripts/audit/`)

| Скрипт | Что делает |
|---|---|
| `inputs.py` | единственный парсер входов: `load_jobs()` (без needs), `load_needs()`, `load_catalog()`; поддержка `AUDIT_ROOT` для фикстур |
| `merge.py` | сверка дериваций с матрицей → `findings.json` |
| `apply.py`, `apply-tiers.py`, `apply-attacks.py` | применение решений: находки, ярусы, атаки |
| `resplit-traces.py` | переразложение трасс по ярусам (`cells` ≡ needs, `hardens_cells` ≡ hardens) |
| `validate-traces.py` | гейт: трассы ≡ матрица (стоит в `pack.sh`) |
| `certify.py` | 8 тестов + отчёт `derivation/certification-report.md` |
| `echo-check.py` | гейт эха вопрос↔вариант (в `pack.sh` ПОКА НЕ включён) |
| `*-workflow.js` | шесть агентных контуров: деривация, деривация-порог, судьи, типизация, консолидация, атаки, судьи атак |
| `*-args-prep.py` | сборщики аргументов для workflow |

**Как запускать агентный контур:** аргументы «запекаются» в копию workflow-скрипта
(`const ARGS = {...}` после блока `meta`, замена `args.` → `ARGS.`), копия кладётся в
`.superpowers/sdd/2026-08-13-model-audit-plan/`, вызывается `Workflow({scriptPath})` без `args`.
**Обязательно** проверять рендер промпта узлом до запуска — однажды промпт ушёл
неинтерполированным (агенты доложили и отказались судить, вердикты не применялись).

---

## 6. Правила, принятые по ходу (держать в силе)

- **Канон — методология Дениса Смирнова** (`~/Agentic/Projects/АНД/AI/Методичка/{and} {ai} {plan}
  учебная программа практикума ai-native orgs – 2026-08-12.md`). При расхождении AIST POS
  приводится к канону; отклонения допустимы только ради пользовательского слоя и называются явно.
- **Не держать два источника одного уровня.** Двойные записи устраняются, а не сверяются гейтами
  (так убраны `caps[]` из шапки рубрики). Гейт ставится там, где источник один.
- **Дыры принимаются явно**, через `derivation/accepted-gaps.json`: зарегистрированная = ⚠,
  незарегистрированная = ✗ и роняет сертификацию.
- **Пользовательский контент пишется с Виктором.** Формулировки, рецепты, гайды — предложения,
  не автоприменение.
- **Числовой гейт `pack.sh` ловит двусмысленные формулировки** в документах («8 способностей»,
  «в четырёх направлениях из пяти») — переформулировать, а не отключать гейт.

---

## 7. Честные результаты, которые нельзя приукрашивать

- **Сходимость деривации:** 48/51 ячейки порога (94,1%) выведены независимо ≥2 агентами из 3.
- **Воспроизводимость (тест 8):** 92% (`draft`) и 53% (`followup`) по Жаккару на повторном прогоне.
  Разбор: расхождение почти целиком про **ярус**, а не про содержание — у `draft` единственная новая
  способность уже размещена в ярусе надёжности; у `followup` 4 из 7 новых уже размещены
  (3 гигиена + 1 надёжность), по-настоящему новых претензий 3.
- **Чего утверждать нельзя:** что независимый прогон даст ту же матрицу ячейка-в-ячейку. У вопроса
  «минимальный набор для первой ценности» несколько правильных ответов; одиночный опрос трёх
  агентов даёт согласие 50–60%. Матрица произведена состязательной процедурой, а не опросом.
- **Грамматика ступеней** подтверждена на 82 формулировках из 90 (91%) — она восстановлена из
  рубрики, а не навязана ей.

---

## 8. Карта артефактов

**Публичное (пакуется с навыком):** `docs/methodology.md`, `docs/level-grammar.md`.

**Внутреннее (в пакет не уезжает, но лежит в репо как доказательство):**
- `derivation/{job}.json` × 13 — канонические трассы: операции, ячейки порога, ячейки надёжности;
- `derivation/raw/*.json` × 39 — сырые слепые деривации;
- `derivation/findings.json` — 104 находки с вердиктами, ярусами, решениями;
- `derivation/attacks.json` — 100 атак с вердиктами и решениями;
- `derivation/catalog-gaps.json` — 31 дыра каталога, 9 тем, все `defer`;
- `derivation/accepted-gaps.json` — реестр принятых дыр лестницы;
- `derivation/certification-report.md` — отчёт сертификации (8 тестов);
- `derivation/criterion.md` — критерий отбора задач (утверждён Виктором);
- `derivation/rubric-edits-proposed.md` — 21 предложенная правка + решение по `context_seed`;
- `derivation/scoring-defense.md`, `derivation/positioning.md` — черновики защиты;
- `derivation/repro-derive.json`, `derivation/rerun-entry.json` — данные теста воспроизводимости.

---

## 9. Первый ход следующей сессии

Спросить Виктора про **предмет способности «Вводные под задачу»** (раздел 3.1) — от него зависят
две правки из пачки. Дальше по порядку раздела 3. После решений — процедура выката (раздел 4).

Если Виктор захочет сразу выкат без правок формулировок: шаги 6–8 раздела 4 выполнимы отдельно,
но гейт эха тогда не включается (останется 13 пар с дословным повтором).
